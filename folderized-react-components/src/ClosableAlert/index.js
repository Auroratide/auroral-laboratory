import container from './container';
import ClosableAlert from './component';
import './style';

/**
 * This is the ClosableAlert component's entrypoint. It serves as the junction
 * for all the other files that compose the component.
 */

export default container(ClosableAlert);